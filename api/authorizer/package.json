{
  "name": "authorizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cargo lambda build --release --arm64",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package": "sam package --template-file template.yaml --s3-bucket $S3_BUCKET --s3-prefix url --output-template-file ./template.package.yaml",
    "package:local": "sam package --template-file template.yaml --s3-bucket monster-of-week-code/local --s3-prefix url --output-template-file ./template.package.yaml",
    "deploy": "npm run build && npm run package:local && sam deploy"
  },
  "author": "",
  "license": "ISC"
}
