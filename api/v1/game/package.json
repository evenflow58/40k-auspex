{
  "name": "game",
  "version": "1.0.0",
  "description": "Endpoints related to games",
  "main": "index.js",
  "scripts": {
    "build": "npm-run-all --parallel build:*",
    "build:create": "cd create && npm run build",
    "build:update": "cd update && npm run build",
    "build:get": "cd get && npm run build",
    "package": "npm-run-all --parallel package:*",
    "package:create": "cd create && npm run package",
    "package:update": "cd update && npm run package",
    "package:get": "cd get && npm run package"
  },
  "author": "",
  "license": "ISC"
}
