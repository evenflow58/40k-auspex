{
  "name": "processing",
  "version": "1.0.0",
  "description": "Processing lambda",
  "main": "index.js",
  "scripts": {
    "build": "npm-run-all --sequential build:*",
    "build:load-data": "cd load-data && npm run build",
    "build:drop-files":  "cd drop-files && npm run build",
    "package": "npm-run-all --sequential package:*",
    "package:load-data": "cd load-data && npm run package",
    "package:drop-files": "cd drop-files && npm run package"
  },
  "author": "",
  "license": "ISC"
}
